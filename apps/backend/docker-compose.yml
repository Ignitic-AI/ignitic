version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: backend-postgres
    environment:
      POSTGRES_DB: backend_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/migrations:/docker-entrypoint-initdb.d
    networks:
      - backend-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
  
  # Backend Application (commented out - running directly)
  # backend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: backend-app
  #   environment:
  #     # Server Configuration
  #     SERVER_PORT: 8080
  #     ENVIRONMENT: development
  #     
  #     # Database Configuration
  #     DB_HOST: postgres
  #     DB_PORT: 5432
  #     DB_USER: postgres
  #     DB_PASSWORD: postgres123
  #     DB_NAME: backend_db
  #     DB_SSL_MODE: disable
  #     
  #    
  #     # Security Configuration
  #     JWT_SECRET: your-secret-key-change-this-in-production
  #     ENCRYPTION_KEY: your-encryption-key-32-bytes-long-change-this
  #     RATE_LIMIT_RPS: 100
  #     
  #     # Email Configuration
  #     SENDGRID_API_KEY: your-sendinblue-api-key-here
  #     SENDER_EMAIL: noreply@yourapp.com
  #     SENDER_NAME: Your App
  #     FRONTEND_URL: http://localhost:3000
  #     
  #     # Backend Configuration
  #     backend_MAX_WORKERS: 10
  #     backend_TASK_TIMEOUT: 300
  #     backend_COMPLIANCE_MODE: SOC2_GDPR
  #     backend_AUTOMATION_ENABLED: true
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - backend-network
  #   restart: unless-stopped

volumes:
  postgres_data:

networks:
  backend-network:
    driver: bridge 